
    {
      sortedArticles.length > 0 ? (
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {sortedArticles.slice(0, pageLimit).map((post ) => (
            <ArticleCard post={post} baseUrl={`/${category}/${subcategory}`} />
          ))}
        </div>
        ) 
        : (
        <div class="py-12 text-center">
          <p class="text-xl text-gray-600">
          No {subcategory} posts available at the moment.
          </p>
        </div>
      )
    }

    {
    sortedArticles.length === 0 && 
    (
        <div class="rounded-lg bg-gray-50 p-8 text-center">
          <h3 class="mb-2 text-lg font-medium text-gray-900">
            No articles yet
          </h3>
          <p class="text-gray-600">
            We're currently working on content for this section. Check back soon
            for updates!
          </p>
        </div>
      )
    }
